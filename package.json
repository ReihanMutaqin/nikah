{
  "name": "rdir-studio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd Wedding/app && npm install && npm run build && cd ../.. && mkdir -p dist/wedding && cp dashboard.html dist/index.html && cp -r Wedding/app/dist/. dist/wedding/"
  }
}
